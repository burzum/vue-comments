import commentsStore from '@/store/store';

describe('commensStore', () => {
	let store;

	beforeEach(() => {
		store = commentsStore;
	});

	it('equals messages to ["Cat"]', () => {
		//console.log(store);
	});
});
